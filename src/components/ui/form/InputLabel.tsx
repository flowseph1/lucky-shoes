import React from "react";

const InputLabel = ({ label }: { label: string }) => {
    return <div className="mb-2 text-xs font-semibold text-text-extra-light">{label}</div>;
};

export default InputLabel;
