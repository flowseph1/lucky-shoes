import { Title } from "@/components/ui";
import React from "react";

const AdminCategorias = () => {
    return (
        <div className="flex-1 overflow-y-auto ">
            <Title subtitle="Categorias" className="mb-12" />
        </div>
    );
};

export default AdminCategorias;
